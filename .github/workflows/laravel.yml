name: Laravel CI

on:
  push:
    branches: [ lesson-29 ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: shivammathur/setup-php@15c43e89cdef867065b0213be354c2841860869e
      with:
        php-version: '8.0'
    - uses: actions/checkout@v2
    - name: Copy .env
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"
    - name: Get composer cache directory
      id: composer-cache
      run: |
        cd ./dist
        echo "::set-output name=dir::$(composer config cache-files-dir)"

    - name: Cache composer dependencies
      uses: actions/cache@v2
      with:
       path: ${{ steps.composer-cache.outputs.dir }}
       # Use composer.json for key, if composer.lock is not committed.
       # key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.json') }}
       key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
       restore-keys: ${{ runner.os }}-composer-
       
    - name: Install Dependencies
      run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
    - name: Generate key
      run: php artisan key:generate
    - name: Install Node Modules
      run: npm install
    - name: Directory Permissions
      run: chmod -R 777 storage bootstrap/cache
    - name: Build SASS
      run: npm run prod
 #   - name: Create Database
 #    run: |
 #       mkdir -p database
 #       touch database/database.sqlite
 #   - name: Execute tests (Unit and Feature tests) via PHPUnit
 #     env:
 #       DB_CONNECTION: sqlite
 #       DB_DATABASE: database/database.sqlite
 #     run: vendor/bin/phpunit
  deploy-to-test:
    needs: build
    
    runs-on: self-hosted
    steps:
      - name: Login to Docker Container
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - run: |
          docker pull marinaostaa/demo-app:latest
          docker stop myXampp
          docker rm myXampp
          #docker run -d -p 8080:8080 --name myXampp marinaostaa/demo-app:latest
          docker run -d -p 41061:22 -p 41062:80 --name myXampp  marinaostaa/demo-app:latest
